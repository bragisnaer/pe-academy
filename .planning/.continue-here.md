---
context: default
phase: ad-hoc — quiz integrity + lesson gating
task: null
total_tasks: 8
status: paused
last_updated: 2026-04-16T12:19:02.445Z
---

<current_state>
Quiz randomization infrastructure complete. All uncommitted. Next session implements quiz session locking, splash page, tab-switch detection, mark-complete timer+scroll, and sequential lesson gating.
</current_state>

<completed_work>

1. Result page — all hardcoded strings replaced (Level N Unlocked, dynamic pass mark from level_gates, dead intermediate-placeholder link → /dashboard)
2. selectPerTopic() — 4 questions/topic for L1-10, 2/topic for L11 (auto-derived from topic count)
3. LEVEL_UNLOCK_MAP — extended to all 11 levels in submit route
4. scripts/import-quiz-questions.js — reads content/quiz/level-{N}/*.json, validates, generates SQL migration
5. Level 1 SQL (200q) generated + user ran it in Supabase SQL Editor ✓
6. Level 2 SQL (200q) generated — NOT YET RUN in Supabase
7. level_gates pass marks set by user: 70/75/75/75/80/80/80/85/85/85/90% ✓
8. Quiz page — Level 1 no longer requires user_progress row (always accessible)
9. Lesson page — fixed redirect("/levels/beginner/quiz") → redirects to correct prev-level quiz slug
10. Sidebar — fixed /levels/beginner/quiz → /levels/foundations/quiz
11. content/quiz/ folder structure created (level-1 through level-11 subfolders)
</completed_work>

<remaining_work>

HUMAN ACTION FIRST:
- Run supabase/migrations/20260416121330_level2_quiz_questions.sql in Supabase SQL Editor

CODE TASKS (implement in this order):

1. **DB migration** — ALTER quiz_attempts:
   - ADD COLUMN status TEXT NOT NULL DEFAULT 'completed' (backwards compat)
   - ADD COLUMN question_ids JSONB (the locked 20 question IDs)
   - ADD COLUMN tab_switches INTEGER NOT NULL DEFAULT 0
   - ALTER COLUMN completed_at DROP NOT NULL (null = in-progress)
   - Write to supabase/migrations/TIMESTAMP_quiz_attempts_session.sql

2. **POST /api/quiz/start** (new route):
   - Auth + level access check
   - Run selectPerTopic() server-side → 20 question IDs
   - INSERT quiz_attempts with status='in_progress', question_ids=[ids], completed_at=NULL
   - Return {attempt_id, questions} (questions without correct_index)

3. **Quiz splash page** — rewrite app/levels/[levelSlug]/quiz/page.tsx:
   - Show: level name, pass mark, question count, rules list
   - Rules: complete in one sitting, no tab switching, no going back
   - "Start Quiz →" button POSTs to /api/quiz/start, redirects to /quiz/attempt?id=xxx
   - Must be a client component (needs fetch + redirect)

4. **Quiz attempt page** — NEW app/levels/[levelSlug]/quiz/attempt/page.tsx:
   - Load attempt by ?id= param, verify status='in_progress' + user_id match
   - Fetch the specific question IDs stored in attempt.question_ids
   - Render QuizForm with attemptId prop

5. **QuizForm updates**:
   - Accept attemptId prop (passed to submit route)
   - Add useEffect for visibilitychange event listener
   - State: tabSwitchCount (int), showTabWarning (bool)
   - On 1st switch: show prominent warning banner (doesn't block)
   - On 3rd switch: auto-submit with current answers (partial if unanswered)
   - Pass tab_switches count to submit route

6. **Submit route updates**:
   - Accept attemptId in body
   - Verify attempt exists, belongs to user, status='in_progress'
   - UPDATE attempt: status='completed', score, total, passed, answers, completed_at=now(), tab_switches=N
   - (Rest of logic unchanged)

7. **MarkCompleteButton** — two conditions before enabling:
   - 30s countdown timer (useEffect + setInterval)
   - Scroll depth: IntersectionObserver on a sentinel div at lesson bottom
   - Button disabled until BOTH: timer elapsed AND scrolled past 90%
   - Show countdown "Available in Xs" while waiting
   - Sentinel div must be added to lesson page near Mark Complete button

8. **Lesson page** — sequential gate:
   - Add export const dynamic = 'force-dynamic'
   - After auth check, find previous lesson in same module
   - If previous lesson exists + NOT in completedLessonIds → redirect('/dashboard')
   - getCompletedLessons() already exists in lib/actions/lessons.ts

9. **LessonSidebar** — sequential locking:
   - Find index of first incomplete lesson per module
   - Lessons before first incomplete: accessible (completed)
   - First incomplete: accessible (current)
   - Lessons after first incomplete: isLocked=true
   - Already has isLocked prop on LessonSidebarItem — just pass correct value
</remaining_work>

<decisions_made>

- Quiz flow: /levels/[slug]/quiz = splash page, /levels/[slug]/quiz/attempt?id=xxx = live quiz
- Session locking via DB (not sessionStorage) — refresh exploitability eliminated
- Tab switches: warn banner on 1st, auto-submit on 3rd (user confirmed)
- Mark complete: 30s timer AND scroll-to-90% — both required
- Sequential lesson gate: server-side, force-dynamic, redirect to dashboard on violation
- Sidebar: only first incomplete lesson accessible; completed = always accessible; future = locked
- L11 quiz: 10 topics × 2 questions (auto-derived by Math.floor(20/topicCount))
- Import script generates SQL migration files → user pastes into Supabase SQL Editor
- Pass marks: 70/75/75/75/80/80/80/85/85/85/90% (set in level_gates table ✓)
- No retake cooldown — randomized questions are sufficient anti-cheat
</decisions_made>

<blockers>
None blocking code. One pending human action (Level 2 SQL).
</blockers>

## Required Reading (in order)
1. `lib/types/quiz.ts` — needs QuizAttempt type updates (status, question_ids, tab_switches)
2. `components/quiz-form.tsx` — understand current state before adding tab detection
3. `components/mark-complete-button.tsx` — understand before adding timer+scroll
4. `components/lesson-sidebar.tsx` — understand before adding sequential lock logic
5. `app/lessons/[moduleSlug]/[lessonSlug]/page.tsx` — understand before adding force-dynamic + sequential gate

## Infrastructure State
- Supabase: Level 1 questions (200) + level_gates pass marks in DB ✓
- Supabase: Level 2 questions (200) SQL generated, NOT YET RUN
- content/quiz/level-1/ — 5 JSON files (200q total) ✓
- content/quiz/level-2/ — 5 JSON files (200q total) ✓
- content/quiz/level-3 through level-11/ — folders exist, empty (awaiting content)

<context>
All quiz infrastructure is solid. The remaining work is purely UX/integrity: making the quiz session tamper-resistant, enforcing reading discipline on lessons, and adding the pre-quiz briefing. The DB migration for quiz_attempts is the critical first step — everything else (start route, attempt page, QuizForm tab detection, submit route) depends on it.
</context>

<next_action>
Write DB migration for quiz_attempts schema changes (status, question_ids, tab_switches, nullable completed_at), then build /api/quiz/start route, then quiz splash page, then attempt page, then QuizForm tab detection, then submit route update, then MarkCompleteButton timer+scroll, then lesson sequential gate, then sidebar locking.
</next_action>
