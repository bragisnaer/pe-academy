---
phase: 08-marketing-site
plan: 08-01
type: feature
wave: 1
depends_on: []
files_modified:
  - app/(marketing)/layout.tsx
  - app/(marketing)/page.tsx
  - components/public-nav.tsx
  - components/marketing-footer.tsx
  - app/robots.ts
  - app/sitemap.ts
  - supabase/migrations/20260415000001_anon_news_select.sql
autonomous: true
requirements:
  - MKT-01
  - MKT-02
  - MKT-03
  - MKT-04
  - MKT-05
  - MKT-06
  - MKT-07
must_haves:
  truths:
    - "A logged-out user visiting / sees the marketing homepage — not a redirect to /login"
    - "The public nav renders Logo, Features, Pricing, Sign In (ghost), and Get Started (primary) links on every marketing page"
    - "The homepage hero contains an outcome-specific headline, sub-headline, and a Get Started Free CTA above the fold"
    - "A live strip of recent PE deal headlines from /news data is visible on the homepage without requiring login"
    - "The page returns valid Open Graph and title metadata"
  artifacts:
    - path: "app/(marketing)/page.tsx"
      provides: "Marketing homepage with hero, news teaser, features, social proof"
      contains: "metadata export, auth redirect, news_articles query"
    - path: "components/public-nav.tsx"
      provides: "PublicNav with all required links and ThemeToggle"
      contains: "PublicNav"
    - path: "app/(marketing)/layout.tsx"
      provides: "Marketing route group layout wrapping all public pages"
      contains: "PublicNav"
  key_links:
    - from: "app/(marketing)/layout.tsx"
      to: "components/public-nav.tsx"
      via: "PublicNav import"
      pattern: "PublicNav"
    - from: "app/(marketing)/page.tsx"
      to: "supabase/migrations/20260415000001_anon_news_select.sql"
      via: "anon RLS on news_articles"
      pattern: "news_articles"
---

<objective>
Marketing homepage at / is publicly accessible without authentication. Includes PublicNav, hero section, live PE news teaser, features grid, and valid OG metadata. Implementation was committed in three commits (1e910c6, 6d98129, 23d420b) before GSD planning was set up — this plan documents the work for tracking and verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-marketing-site/08-CONTEXT.md

Phase 8 is fully implemented. This plan exists for GSD tracking. No code changes needed — executor should verify the implementation against success criteria and write SUMMARY.md.
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase anon role → news_articles | SELECT-only RLS policy; no write access for unauthenticated users |
| User → auth redirect | Server-side auth.getUser() check; cannot be bypassed client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | news_articles anon SELECT | accept | PE deal news is public information; no PII in news_articles table |
| T-08-02 | Spoofing | auth redirect in page.tsx | mitigated | Uses server-side createClient() + auth.getUser(); client cannot spoof server session |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Verify existing implementation against Phase 8 success criteria</name>
  <files>
    app/(marketing)/layout.tsx
    app/(marketing)/page.tsx
    components/public-nav.tsx
    components/marketing-footer.tsx
    app/robots.ts
    app/sitemap.ts
  </files>
  <action>
    Phase 8 is already fully implemented. Verify each success criterion against the code:

    SC-1: `app/(marketing)/page.tsx` — logged-in users redirect to /dashboard (server-side), logged-out users see homepage.
    SC-2: `components/public-nav.tsx` — check it contains Logo ("PE Academy"), Features link (/#features), Pricing link (/pricing), Sign In (ghost/ghost variant), Get Started (default variant).
    SC-3: `app/(marketing)/page.tsx` — check for hero section with headline, sub-headline, and /signup CTA above the fold.
    SC-4: `app/(marketing)/page.tsx` — check for news_articles query with anon RLS; verify `supabase/migrations/20260415000001_anon_news_select.sql` exists.
    SC-5: `app/(marketing)/page.tsx` — check for `metadata` export with `openGraph` title/description and `twitter.card`.

    No code changes needed. Write SUMMARY.md documenting verification results.
  </action>
  <verify>
    <automated>grep -n "openGraph\|twitter" "app/(marketing)/page.tsx"</automated>
    Expected: metadata export with openGraph and twitter fields present.
  </verify>
  <done>
    All 5 success criteria verified against existing code. SUMMARY.md written.
  </done>
</task>

</tasks>

<verification>
1. `app/(marketing)/layout.tsx` imports and renders PublicNav and MarketingFooter.
2. `components/public-nav.tsx` contains all required nav links (Logo, Features, Pricing, Sign In, Get Started).
3. `app/(marketing)/page.tsx` exports metadata with openGraph fields.
4. `app/(marketing)/page.tsx` queries news_articles with a limit of 4.
5. `supabase/migrations/20260415000001_anon_news_select.sql` exists (anon RLS).
</verification>

<success_criteria>
- All 5 ROADMAP.md Phase 8 success criteria verified against existing implementation.
- SUMMARY.md written and committed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-marketing-site/08-01-SUMMARY.md` using the standard summary template.
</output>
